/**
 * DZPortfolio Motion System.
 *
 * This directory contains the complete motion architecture for the application,
 * split into distinct modules for maintainability.
 *
 * - `constants`: Spring, easing, and stagger values.
 * - `transitions`: Reusable framer-motion transition configurations.
 * - `variants`: All Variants definitions (fade, scale, nav, footer, etc.)
 * - `presets`: Ready-to-use prop objects bundling variants + transitions.
 */

export * from './constants'
export * from './transitions'
export * from './variants'
export * from './presets'
